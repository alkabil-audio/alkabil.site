# Netlify-only (GitHub Pages ignores this file; there /merch is covered by
# the meta-refresh page at merch/index.html instead).
# Old Squarespace paths that have no static equivalent here yet:
/merch   https://alkabilaudio.bandcamp.com/   302
/cart    /                                    302
